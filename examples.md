object vs subject
  "move this to here" -- what is what
  "move task do done"
  "remove milestone"

non-active context
  "move this to there"
  "move both of those tasks to done" -- preserve context over time
    this is something you could never do before (unless multi-select)

"undo"

"create a new task"

"find ... task and assign `me`"

---

static, implied, implicit context
- current project/scope
- current user and team

exact hover vs proximity-based vs gesture (circling)

---

ambiguities in cascade of targets
- use specifiers "remove this task"

ambiguities in subject vs object "remove milestone"
- "remove milestone from task" vs "delete milestone from project"

use a kbd button to activate the "action mode"

ako nemas dugmice u UI, kako onda naucis kontrole i opcije?
- "--help" sa audio koji ti pokaze
- "can I move"...